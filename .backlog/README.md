# Backlog — Skynet-IADS (VEAF)

Per-lot backlog. Active lots are directories under `.backlog/<LOT-ID>/`, each holding a `PRD.md` and
one file per ticket under `tickets/`. Completed lots are compacted into
`.backlog/archive/<LOT-ID>.md`. This index is the source of truth for **scope and status**, and it
is maintained by hand.

The convention is the one used by [VEAF-Mission-Creation-Tools](https://github.com/VEAF/VEAF-Mission-Creation-Tools)
and [CTLD](https://github.com/VEAF/CTLD), so that anyone moving between the three repositories finds
the same shape.

## Legend

- **Status**: ⬜ ready · 🔄 in-progress · 🧑 waiting-human · ⏸ paused · ✅ done · 🚫 wontfix
- ⏸ **paused** is *deliberately parked*, not blocked: unlike 🧑 nothing is expected of anyone, and
  unlike ⬜ an agent should not pick it up.

## Active lots

| Lot | Status |
|-----|--------|
| [FEAT-LAST-LINE-OF-DEFENSE](FEAT-LAST-LINE-OF-DEFENSE/PRD.md) — **a SAM held by the network cannot notice the aircraft overhead, and coverage is never refreshed.** Reported from a live VEAF session on 2026-09-17: fly under the EWRs' radar horizon and no battery reacts whatever the distance, then destroy the EWRs and everything reverts to the DCS AI and engages. Measured on that log: 7 933 SAM status lines dark under network control, **zero** ever lit in 24 minutes. Adds a short virtual detection radius that wakes a dark site on close proximity, and a periodic coverage refresh — because the incremental rebuild used for a moving AWACS never purges, so an aircraft in transit accumulates every battery it has flown near and holds them all non-autonomous. **Both tickets are done and merged**: the code, its 31 tests (18 + 13, almost all driving the real cycle rather than the wake-up directly) and the documentation are on `develop`. **Measured in DCS on 2026-09-19** through VEAF's dcs-bridge, on the mission this lot leaves behind (`unit-tests/last-line-of-defence/skynet-insim-last-line-of-defence.miz`, under `demo-missions/` until 2026-09-20): a battery the network held dark lit up on proximity with no radar contact anywhere, stayed in the network while lit, and fell silent 45 s — to the second, twice — after the intruder left its radius; and a battery whose only parent was an AWACS was handed back the moment that AWACS flew past its own detection range | ✅ |
| [FIX-STALE-HARM-SILENCE](FIX-STALE-HARM-SILENCE/PRD.md) — **a site cleaned up while it is evading an anti-radiation missile is deaf for the rest of the mission.** `cleanUp()` cancels the HARM timers but leaves `harmSilenceID` set, and nothing will ever clear it — the task that would have is the one just removed. `goLive()` refuses while that field is set, so the site is closed to network designation, to autonomy, **and to the last line of defense and `reportContact` added in FEAT-LAST-LINE-OF-DEFENSE**. Reached by any `addSAMSitesByPrefix()` call, which VEAF missions make on respawn. From [issue #3](https://github.com/VEAF/Skynet-IADS/issues/3), in walder's original. **Ticket 02 was added while checking that premise**: a `*ByPrefix` call leaves every element it discards wired into the coverage graph, so a battery keeps a dead EW radar as a valid parent and stays dark under nobody's watch. Merged as [PR #18](https://github.com/VEAF/Skynet-IADS/pull/18), issue #3 closed | ✅ |
| [FIX-COVERAGE-UPDATE-DARKENS-SITES](FIX-COVERAGE-UPDATE-DARKENS-SITES/PRD.md) — **declaring that a radar covers a battery switches the battery off.** `buildRadarAssociation()` records the link through `addParentRadar()`, which ends in `informChildrenOfStateChange()` and therefore `goDark()` — so adding an EW radar during a mission darkens every battery in its range, including one designated onto an intruder and not yet locked on, for one contact cycle. It is also 250 state notifications during an `activate()` where 10 are needed. `FEAT-LAST-LINE-OF-DEFENSE` wrote `addParentRadarWithoutStateChange()` for exactly this and converted the other caller; this is the one it missed. Found while instructing FIX-STALE-HARM-SILENCE ticket 02 | ✅ |
| [CHORE-PROFESSIONALIZE-THE-REPO](CHORE-PROFESSIONALIZE-THE-REPO/PRD.md) — **bring this repository up to the standard of the other two VEAF projects.** It already has a Lua 5.1 test suite and a CI that runs it; it has no backlog, no changelog, no agent instructions, no automated release, no static analysis, no published documentation, and its build script cannot run on the CI runner. The legacy in-sim suites are still waiting to be migrated. **All seven tickets are done** — gitflow, the backlog and changelog, the build and release workflows, luacheck and stylua, the published MkDocs site, the hygiene pass, and the migration of the legacy suites. Ticket 04 closed on 2026-09-20: `abstract-radar-element` ported in four slices (all 46 of its live tests), **six legacy suites removed from both copies** — the loose `unit-tests/*.lua` and the one inside `skynet-unit-tests.miz` — and a CI job that checks that archive, which no gate had ever looked at. Test coverage 91.17% → **94.63%**. Verified in DCS on 2026-09-20: the in-sim suite runs 118 tests where it ran 168, exactly the 50 removed, with no script-loading error. Its 4 remaining failures pin DCS figures last updated in 2023 and are recorded in `docs/evolutions.md` under *The in-sim suite drifts, and nothing says so*, whose open question they answer, and are ticket 03 of `FIX-IN-SIM-SUITE-TESTS-A-2023-SKYNET` | ✅ |
| [FIX-SETUP-WARNINGS-AND-LOG-NOISE](FIX-SETUP-WARNINGS-AND-LOG-NOISE/PRD.md) — **Skynet says nothing to the one person who needs it, and something to everyone that helps nobody.** A mistyped group name is the commonest setup mistake there is, and the three places that catch it build a warning and hand it to `printOutputToLog()`, which takes one argument and drops the flag — so nothing reaches the screen and the only trace is a line in a log nobody opens until something is already wrong. Meanwhile every unit that spawns writes `New Object Spawned` through `env.info` with no `SKYNET:` prefix, twice over on a mission with two networks, for an enrolment that is commented out. Both found while writing the tests for CHORE-TEST-COVERAGE-FLOOR, both pinned by tests that say a fix is what turns them red. Merged as [PR #25](https://github.com/VEAF/Skynet-IADS/pull/25); the screen warnings turn out to reverse a deliberate 2020 choice of walder's rather than repair an accident, which the PRD now records | ✅ |
| [FIX-JAMMER-SILENCE-OUTLIVES-THE-JAMMER](FIX-JAMMER-SILENCE-OUTLIVES-THE-JAMMER/PRD.md) — **the silence does not outlive the jammer after all.** Opened on the claim that a battery a jammer had put on weapon hold was never handed back, so an autonomous one held fire for the rest of the mission. Wrong: `evaluateIfTargetsContainHARMs` has always released it ten seconds after the jammer goes quiet, on a scan every live element runs every two seconds — measured against `develop` with no new code. What was true is that the path had **no test**, which is why reading the jammer did not reveal it; it has two now, and `api.md` states the delay for the first time. The lot still carries three agreed clean-ups: `addJammer()` removed, a guard on `addRadioMenu()`, and one `jam()` per site instead of one per radar Merged as [PR #27](https://github.com/VEAF/Skynet-IADS/pull/27); ticket 01 closed 🚫 wontfix, and the coverage report was fixed along the way — `luacov` adds to an existing stats file, so measuring twice without clearing it reports the union of both runs | ✅ |
| [CHORE-TEST-COVERAGE-FLOOR](CHORE-TEST-COVERAGE-FLOOR/PRD.md) — **the suite runs in CI, and nobody knows what it covers.** It measured nothing until this lot: no `luacov`, no report, no threshold. **70.22% → 91.17%**, with a floor that only goes up, now at **91**, and CI failing below it on every pull request. The climb was the logger's status printers (ticket 03), the network facade every mission maker writes against (05) and the long tail (06); ticket 04 closed as no longer needed once the floor passed 90 without it. What stays uncovered, and why, is written down in `test/lua/README.md` | ✅ |
| [FIX-IN-SIM-SUITE-TESTS-A-2023-SKYNET](FIX-IN-SIM-SUITE-TESTS-A-2023-SKYNET/PRD.md) — **the in-sim mission has been checking code we stopped shipping.** `unit-tests/skynet-unit-tests.miz` carries its own copy of the deliverable, and it is `3.3.0-develop`, built **29.12.2023**; the `.miz` had not been touched between 2023-12-30 and 2026-09-20. So the in-sim suite has been exercising a Skynet from three years ago, and cannot catch a regression in the one we ship — which is the guarantee `CHORE-PROFESSIONALIZE-THE-REPO` ticket 04 left six suites in there on. It also drags MiST in: `fe40c4a` took MiST out of Skynet on 2026-08-30, but the 2023 artifact still makes 33 MiST calls, so the mission loads it, MiST installs its own event handler, and a `DEAD` event for an object it does not know puts **a popup on the player's screen**. Five MiST calls in the harness are all that would be left, and each already has a `SkynetIADSUtils` equivalent. Found on 2026-09-20 from David's DCS run of the mission. Five tickets, all coded; **119 tests, 0 failures** in DCS on 2026-09-20 at 16:17, with no `ERROR SCRIPTING` line and no MiST anywhere in the log, and no regression in Skynet across the three years. The missions are **assembled** now rather than carrying a committed copy (ticket 05, David's idea), and ED's own figures left the suite for `test/lua/dcs-figures.lua`, watched by a weekly workflow. [PR #32](https://github.com/VEAF/Skynet-IADS/pull/32) | ✅ |
| [FIX-DEMO-MISSIONS-SHIP-A-2023-SKYNET](FIX-DEMO-MISSIONS-SHIP-A-2023-SKYNET/PRD.md) — **the missions newcomers open run Skynet 3.2.** The same defect as the lot above, one shelf lower: three of the four missions under `demo-missions/` carry `SKYNET VERSION: 3.2 | BUILD TIME: 29.12.2023`, untouched since 2023-12-29, while `develop` ships 3.5.0. These are what somebody downloads to learn what Skynet does, and what `documentation/` points at — so the worked example beside the instructions demonstrates a build from before two minor versions of behaviour changes. Nothing goes red, because nothing here is a test. Their setup scripts drifted too, three of four disagreeing with their loose copy, and all four still load MiST for a single call that has a drop-in replacement. Ticket 03 decided the shape first, on 2026-09-20: **(b)** — the demos are assembled by `miz-suite.py` like the test missions, and the playable copies become release assets. The caveat David took with it is that this repository has published no release yet, so until one is cut a demo is only reachable by cloning and running the build, which the Quick start page now says. Found on 2026-09-20 while closing the lot above | 🔄 |
